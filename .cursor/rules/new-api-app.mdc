---
description: 我正在开发一个跨平台移动应用，基于 GitHub 项目 [https://github.com/tpu01yzx/new-api-app]，主分支为 main，使用 Deploy Key 进行权限控制。该应用旨在支持对多个部署了 [https://github.com/Calcium-Ion/new-api] 项目的实例进行统一监控和管理。
globs:
    功能需求：
        支持添加多个 new-api 实例，每个实例通过 URL 和 Access Token 进行接入；
        查看每个实例的系统运行状态（如健康检查、版本、KEY数量、用户数量、支持模型数量、最近访问的KEY、登录的用户和调用的模型、模型调用token饼形图、模型调用次数饼形图、访问系统的IP地址列表（支持反向查询域名）等）；
        查看异常日志（如报错记录、故障信息等）；
        查看最近的 API 调用情况（如请求频率、失败请求、慢请求等）；
    平台要求：
        应用需兼容 Apple（iOS）、Android 以及华为鸿蒙系统；
        UI 应具备适配多个站点实例的能力，支持动态切换与展示；
    开发流程：
        将当前项目根文件夹初始化为https://github.com/tpu01yzx/new-api-app的项目根目录；
        先安装所需的开发工具；
        然后选择合适的技术框架实现写一次代码同时编译支持多个平台；
        然后将https://github.com/Calcium-Ion/new-api 项目以submodal的形式嵌入到本项目中；
        然后开始正式开发代码，中途出现错误及时解决，然后再推进到下一个步骤：
            确定即将要实现的需求；
            编写代码实现预测；
            使用仿真器调试模式进行测试；
            编译三个平台的安装包；
            测试和编译都通过后将代码提交到git仓库；
            如果开发已经基本完成，则打包发布版本v0.0.1，并随着开发的进度和不断完善，不断升级版本和发布；
            如果问题没有解决完成或者功能还没全部完成，则返回开头重新开始。
    注意事项：
        由于网络限制，无法通过HTTPS访问github.com，请使用SSH协议；        
        开发过程中生成的命令尽量使用绝对路径；
        如果在开发过程中发现缺少什么信息的，及时提示我并中断开发过程，等待我补充完整信息，收到我的命令后再继续开发；
        注意维护一个todo.md文件用于记录开发过程
alwaysApply: false
---
